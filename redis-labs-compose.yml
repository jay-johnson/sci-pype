version: '2'

services:

  jupyter:
    image: jayjohnson/jupyter:2.0.4
    container_name: "jupyter"
    hostname: "jupyter"
    env_file: 
      - ./justredis/redis-labs.env
    ports:
      - "82:8888"
    volumes:
      - ./env:/opt/work/env
      - ./configs:/opt/work/configs
      - ./examples:/opt/work/examples
      - ./bins:/opt/work/bins
      - /opt/project:/opt/project
      - /opt/work:/opt/work
    network_mode: "host"

